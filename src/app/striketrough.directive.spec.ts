import { StriketroughDirective } from './striketrough.directive';

describe('StriketroughDirective', () => {
  it('should create an instance', () => {
    const directive = new StriketroughDirective();
    expect(directive).toBeTruthy();
  });
});
